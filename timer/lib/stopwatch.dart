import 'package:flutter/material.dart';

import 'timer.dart';

class Stopwatch extends StatefulWidget {
  @override
  _StopwatchState createState() => _StopwatchState();
}

class _StopwatchState extends State<Stopwatch> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

//start -> DITTO
//set -> DITTO
//set becomes -> set max stopwatch time
//reset -> DITTO
//in timer -> read time left ---VS--- in stopwatch -> read time passed
//getOriginalTime becomes -> get max stopwatch time
//getTimePassed -> DITTO
//getTimeLeft -> does not exist
//isRunning -> DITTO
//getFormattedDuration -> DITTO
//getStringFromFormattedDuration -> DITTO
//getStringFromDuration -> DITTO
